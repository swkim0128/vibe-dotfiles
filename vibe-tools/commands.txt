# ~/.config/vibe-tools/commands.txt
# 형식: 화면에 보일 이름 | 실행할 명령어
# '#'으로 시작하는 줄은 주석, 빈 줄은 무시됩니다.

# ── AI ──────────────────────────────────────────────
🤖 Claude Code: 현재 프로젝트 분석 | claude -p '이 프로젝트의 구조와 핵심 로직을 분석해서 요약해줘'

# ── Tmux ─────────────────────────────────────────────
🛠️  Tmux 설정 새로고침 | tmux source-file ~/.tmux.conf

# ── 개발 서버 ─────────────────────────────────────────
🌐 로컬 서버 시작 (예시) | npm run dev

# ── Docker ───────────────────────────────────────────
🧹 Docker 사용하지 않는 자원 정리 | docker system prune -af

# ── Neovim ───────────────────────────────────────────
📂 NvChad 설정 폴더로 이동 | cd ~/.config/nvim && nvim .

# ── IPC: 패널 간 클로드 통신 ─────────────────────────────────────────────────
# 사용법: claude-delegate.sh <타겟_패널_ID> "<작업 내용>"
# 타겟 패널 ID 확인: tmux list-panes -a -F '#{pane_id} #{session_name}:#{window_index}.#{pane_index} #{pane_current_command}'
# 예) ~/.config/vibe-tools/claude-delegate.sh '%3' 'auth 모듈 유닛 테스트를 작성해줘'
📡 다른 패널의 클로드에게 작업 위임 | ~/.config/vibe-tools/claude-delegate.sh
