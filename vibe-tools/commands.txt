# ~/.config/vibe-tools/commands.txt
# 형식: 화면에 보일 이름 | 실행할 명령어
# '#'으로 시작하는 줄은 주석, 빈 줄은 무시됩니다.
#
# 정렬 기준: "이 작업을 할 때 손이 가는 순서" — 자주 쓰는 상황 → 드문 상황

# ── 🗂️  세션 시작/전환 ──────────────────────────────────
🗂️  프로젝트 세션 전환 (fzf) | ~/.config/vibe-tools/vibe.sh fzf
🔙 para 세션으로 즉시 이동 | tmux switch-client -t para
🔚 현재 Vibe 세션 종료 → para 복귀 | vibe done

# ── 🤖 Claude / AI ──────────────────────────────────────
🤖 Claude 프롬프트 팝업 | ~/.config/vibe-tools/claude-skills.sh
📊 Claude Code: 현재 프로젝트 분석 | claude -p '이 프로젝트의 구조와 핵심 로직을 분석해서 요약해줘'

# ── 📡 세션 간 통신 (IPC) ───────────────────────────────
# claude-delegate.sh / claude-callback.sh 는 인자가 필수라 팝업에서 직접 실행 불가.
# 아래 명령은 패널/세션 목록을 띄워 다음 단계에서 수동 입력을 돕는 보조 용도.
📋 열린 패널 ID 목록 보기 (IPC용) | tmux list-panes -a -F '#{pane_id} #{session_name}:#{window_index}.#{pane_index} #{pane_current_command}'
📡 세션 목록 보기 (vibe cast용) | tmux list-sessions

# ── 📦 Git / 버전관리 ───────────────────────────────────
📦 Lazygit | lg
🌿 브랜치 목록 확인 | git branch -a
📤 현재 브랜치 푸시 | git push origin HEAD

# ── 📝 Neovim ───────────────────────────────────────────
📂 NvChad 설정 폴더 열기 | cd ~/.config/nvim && nvim .

# ── 🖥️  시스템 상태 확인 ────────────────────────────────
🖥️  시스템 모니터링 (btop) | btop
💾 디스크 사용량 (duf) | duf
📁 폴더 크기 분석 (dust) | dust .
ℹ️  시스템 정보 (fastfetch) | fastfetch

# ── 🐳 Docker ──────────────────────────────────────────
🧹 Docker 미사용 자원 정리 | docker system prune -af

# ── 🛠️  환경 관리 / 백업 ────────────────────────────────
💾 현재 설정 백업 (vibe-dotfiles) | ~/Project/vibe-dotfiles/backup.sh
🛠️  Tmux 설정 새로고침 | tmux source-file ~/.tmux.conf
📋 Vibe 치트시트 보기 | vhelp
